/* ============================================================
   ДАННЫЕ САЙТА, НЕ ЗАВИСЯЩИЕ ОТ ЯЗЫКА — имена латиницей, дата, карта,
   фото, музыка. Все тексты на русском и азербайджанском — в js/i18n.js.
   ============================================================ */

const CONFIG = {

  // Имена молодожёнов латиницей — на экране с вензелем (одинаково на обоих языках).
  // Вензель над этой строкой (S/E с росчерками) — не шрифт, а картинка
  // assets/monogram.png. При смене имён её нужно переделать отдельно —
  // см. комментарий у .monogram-wrap в css/style.css.
  names: {
    en: "SULEIMAN & ELEONORA"
  },

  // Дата и время свадьбы (ISO). Часовой пояс Тюмени указан явно (+05:00).
  date: "2026-10-03T18:00:00+05:00",

  // Локация. address — по нему ищется точка на встроенной Яндекс Карте
  // (поэтому по-русски); подписи адреса для гостей — в i18n.js.
  venue: {
    address: "г. Тюмень, ул. Дружбы, 183",
    mapUrl: "https://yandex.ru/maps/?text=Тюмень,+улица+Дружбы,+183",
    photo: "assets/venue/photo.jpg"
  },

  // Фоновая музыка (mp3). Пусто — кнопка-«динамик» не показывается.
  musicSrc: "assets/TurkMusic.mp3",

  // URL веб-приложения Google Apps Script (см. TELEGRAM_SETUP.md) —
  // пересылает ответы анкеты в Telegram.
  scriptUrl: "https://script.google.com/macros/s/AKfycbybYIaDcgqPiI851sOBIPE32MnRIZ4VKFNi_B_mInARgy6DEQ3_4GSJV1dROz68TpnRtA/exec"
};
